# Used only for `vite build` (production mode) - overrides .env for this
# build. Empty on purpose: the deployed backend serves this same build as
# static files from the same origin, so API calls should be relative, not
# point at a separate host or port.
VITE_API_ORIGIN=
